############################################
## Configuration file Tweets to db Daemon ##
############################################

## 1 ) Url of database webservice to store tweets:
urldatabase = http://localhost:8000

## 2 ) User of database
user = quiltro

## 3) Password for user in DB
password = perroCallejero

## 4) Interval time in seconds for timer
timer_seconds = 5

## 5 ) Port used to setup daemon API
socket_port = 14001

## 6 ) Max connection allowed at same time
max_connection = 5

## 7 ) Tweets collector app
timeout = 5
